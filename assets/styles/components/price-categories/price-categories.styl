$price-category-border-radius = 5px
content-area.price-categories-wrapper
  positioning-root()
  padding 3rem 1rem
  padding-top 5rem
  background-color #2C2542
  background-image linear-gradient(135deg, #2C2542, #19172C)
  overflow hidden
  color #fff
  // padding 2rem

  &:before, &:after, ul:before, ul:after
    content ''
    display block
    width 100%
    height 4rem
    transform rotate(-45deg) translateY(-50%)
    position absolute
    top 0

  &:before, ul:before
    background-color #F8BF63
    left 4rem

  &:after, ul:after
    background-color #EB5D94
    left 9.5rem

  ul:before, ul:after
    width 50%
    top auto
    bottom -3rem

  ul:before
    left 2rem
  ul:after
    left 7.5rem


  h2
    top -1px
    // z-index 1000
    // margin-bottom 0
    // background transparent
    border-bottom none
    background-color transparent
    z-index 999

price-categories
  display block

  ul, li
    margin 0
    padding 0


colorList = #EB5D94 #F8BF63 #3ECC95 #02AAFF

price-category
  positioning-root()
  display block
  color #000

  for bgCol, i in colorList
    li:nth-child({i+1}) &
      z-index 999 - i

  header
    positioning-root()
    display block
    border-radius 0 0 0 3rem
    margin-top -6rem
    z-index 999
    cursor pointer

    &:after
      content ''
      height 4px
      width 30%
      border-radius 4px
      position absolute
      bottom .5rem
      right .5rem
      background-color #fff

    padding 6rem 2rem 1rem 2rem

    li:first-of-type &
      padding-top 2rem
      margin-top 0
      border-radius 1rem 1rem 0 3rem
      border-top 3px solid #2C2542

    li:last-of-type &
      border-bottom 3px solid #2C2542

    for bgCol, i in colorList
      li:nth-child({i+1}) &
        background-color bgCol
        color #fff

    transition height

    h3
      position relative
      font-size 1.8rem
      margin-bottom .3rem
    p
      position relative
      margin-top 0

  ul
    // display none
    positioning-root()
    background-color #fff
    border-radius 0 0 0 3rem
    display none
    overflow hidden
    padding-top 3rem
    transform translateY(-3rem)
    z-index 998

    li
      transform translateY(-100px)
      transition transform .5s

  &.open ul
    display block

  &.active ul
    li
      transform translateY(0px)

  .opening-hint
    text-align center
    margin-left auto
    margin-right auto
    border-radius 0 0 0 3rem
    // padding 1rem
    height 2rem
    background url(/assets/images/tap.svg) no-repeat top center
    background-size contain

    transform translateY(-100px)
    transition transform .2s
    display none

  &.price-category-in-line-of-sight .opening-hint
    display block
  &.price-category-in-line-of-sight.price-category-in-line-of-sight-and-active .opening-hint
    transform translateY(0)

.opened-once .opening-hint
  display none !important
